# Cleanup Swift evaluations

Run each scenario as a read-only review with `cleanup-swift`. Compare the
reviewer's findings with the required and forbidden outcomes in the fixture.

These fixtures test semantic behavior, not exact wording. A passing review must
name a concrete trigger and explain the resulting behavior. Do not award credit
for generic advice such as "add more tests" or "use explicit types."
