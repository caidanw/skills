---
name: test
description: >-
  Placeholder/demo skill for validating skill installation, repository structure, and
  frontmatter parsing. Use when you need a minimal example skill, want to confirm a
  skills repository is wired up correctly, or need a safe stub to replace later with
  real instructions. Triggers on: "test skill", "demo skill", "placeholder skill",
  "verify skills repo", "check skill loading".
---

# Test Skill

This is a deliberately small placeholder skill.

Use it to verify that:
- the `test/` skill directory is discoverable
- `SKILL.md` frontmatter is parsed correctly
- a minimal skill can be installed and loaded end-to-end

## Instructions

When this skill is loaded:
1. Confirm the repository and skill-loading flow are working.
2. Do the smallest possible task needed to validate the setup.
3. Prefer concise output over elaborate guidance.
4. If a real skill is needed, replace this placeholder with task-specific instructions.

## Non-Goals

This skill does not define a testing methodology, framework, or toolchain.
It exists only as a demo/placeholder artifact.
